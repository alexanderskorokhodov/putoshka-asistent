import React from "react";

function GlosView({lectures}){

    let view = lectures.map((v) => {return <div className=""></div>})

    return <div className="container">
    </div>
}

export default GlosView